import React from 'react';
import { SafeAreaView, Text } from 'react-native'; // Import necessary components

const App = () => {
  return (
    <SafeAreaView 
     style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>React Native Fun!</Text>
    </SafeAreaView>
  );
};

export default App;